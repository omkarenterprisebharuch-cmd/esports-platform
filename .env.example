# ===========================================
# ENVIRONMENT VARIABLES FOR VERCEL DEPLOYMENT
# ===========================================
# Copy this file and fill in your actual values in Vercel Dashboard
# Go to: Project Settings > Environment Variables

# ============ DATABASE (PostgreSQL) ============
# For Vercel, use a cloud PostgreSQL provider like:
# - Vercel Postgres
# - Neon (neon.tech)
# - Supabase
# - Railway
# - PlanetScale


# npx tsx scripts/seed-tournaments-db.ts



# Option 1: Individual variables
DB_HOST=your-db-host.com
DB_PORT=5432
DB_NAME=esports_platform
DB_USER=postgres
DB_PASSWORD=your-secure-password
DB_SSL=true

# Option 2: Connection string (some providers use this)
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# ============ JWT SECRET ============
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# ============ CLOUDINARY (Image Uploads) ============
# Get these from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============ EMAIL (SMTP) ============
# For production, use a service like:
# - SendGrid
# - Mailgun
# - Amazon SES
# - Resend
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@yourdomain.com

# ============ PUSH NOTIFICATIONS (Web Push) ============
# Generate VAPID keys: npx web-push generate-vapid-keys
VAPID_SUBJECT=mailto:your-email@domain.com
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key

# ============ APP CONFIGURATION ============
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
NEXT_PUBLIC_SOCKET_URL=https://your-socket-server.com

# ============ NODE ENVIRONMENT ============
NODE_ENV=production

# ============ REDIS CACHE (Optional) ============
# Redis connection URL for caching
# For Redis Cloud: redis://default:<password>@<host>:<port>
# For local Redis: redis://localhost:6379
# For TLS/SSL: rediss://...
REDIS_URL=redis://localhost:6379

# Set to "false" to explicitly disable Redis caching
# REDIS_ENABLED=false
